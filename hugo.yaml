baseURL: https://zeli-b.github.io/

defaultContentLanguage: ko-kr
languageCode: ko-kr
title: Sat

markup:
  tableOfContents:
    endLevel: 6
    ordered: true
    startLevel: 2
  goldmark:
    renderer:
      unsafe: true
    extensions:
      passthrough:
        delimiters:
          block:
          - - \[
            - \]
          - - $$
            - $$
          inline:
          - - \(
            - \)
          - - $
            - $
        enable: true

params:
  math: true

outputs:
  home:
    - HTML
    - RSS
    - JSON
